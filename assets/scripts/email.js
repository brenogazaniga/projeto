
/*const emailEl = document.getElementById('email');

function handleEmailClick() {
  const to = 'suporte@exemplo.com';
  const subject = 'Assunto: Suporte';
  const body = 'Olá,\n\nDescreva aqui o seu problema.\n\nObrigado!';
  
  const mailto = `mailto:${encodeURIComponent(to)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
  window.location.href = mailto; // abre o cliente de e-mail padrão do usuário
}

emailEl.addEventListener('click', handleEmailClick);/*
